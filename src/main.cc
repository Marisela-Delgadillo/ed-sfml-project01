#include<iostream>
#include <SFML/Graphics.hpp>

int main()
{
    sf::RenderWindow window(sf::VideoMode(800, 600), "Platform Game");
    sf::Event event;
    while(window.isOpen())
    {
       
    }
    return 0;
}